# TODO

- [ ] Create report services module: `src/lib/reportsService.ts` using existing mock data (`students`, `measurementRecords`, `feedingRecords`).
- [ ] Update `src/routes/reports.tsx` to generate real previews for all 6 report types using the service functions.
- [ ] Ensure date/grade/section filters affect all report outputs.
- [ ] Ensure preview table shows correct columns and uses `statusColor` badges where relevant.
- [ ] Run `npm test`/`npm run build` (or available) and fix any TypeScript/lint errors.

