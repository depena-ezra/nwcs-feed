# TODO - Fix nwcsfeed.systems 404 / missing fetch handler

## Current findings

- Live endpoint is still 404.
- Root cause is a deploy/build mismatch: Wrangler is redirecting to a generated `dist/client/wrangler.json` and the deploy/build is failing during SSR build.
- Build error from logs: **Could not resolve "./src/router" from "server/index.ts"**.

## Steps

- [ ] Fix module path used by `server/index.ts` to reference the correct router file (likely `src/router.tsx`, not `src/router.ts`).
- [ ] Re-deploy after a clean build so the Worker includes the fetch handler.
- [ ] Make sure CI/build doesn’t use a broken config by ensuring `wrangler.jsonc` deploys from the project root Worker entry.

## Commands to run

- `npm run build`
